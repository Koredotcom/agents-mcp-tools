schemaVersion: 2
service:
  key: arch-mcp-tools
  sourceRoots:
    - packages/mcp-debug/src
    - packages/mcp-debug/bin
variables:
  CODEX_HOME:
    category: direct
    required: false
    description: Overrides the Codex configuration root used only by the explicit Arch guidance installer and uninstaller commands.
    default: ''
